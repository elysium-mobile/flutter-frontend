name: Flutter Multiplatform CI/CD - Firebase Distribution

on:
  push:
    branches:
      - master

jobs:
  # ----------------------------------------------------------------
  # ANDROID
  # ----------------------------------------------------------------
  android-build-distribute:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        id: setup-java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter Doctor
        run: |
          flutter --version
          flutter doctor -v

      - name: Install dependencies
        run: flutter pub get

      - name: Create Configuration File (lib/config.json)
        run: |
          cat <<EOF > lib/config.json
          {
            "BACKEND_BASE_URL": "${{ secrets.BACKEND_BASE_URL }}",
            "API_KEY_GEMINI": "${{ secrets.API_KEY_GEMINI }}",
            "GOOGLE_OAUTH_CLIENT": "${{ secrets.GOOGLE_OAUTH_CLIENT }}",
            "PUBLISHABLE_KEY_STRIPE": "${{ secrets.PUBLISHABLE_KEY_STRIPE }}"
          }
          EOF

      - name: Create google-services.json
        run: |
          cat <<EOF > android/app/google-services.json
          {
            "project_info": {
              "project_number": "301176068632",
              "project_id": "${{ secrets.FIREBASE_PROJECT_ID }}",
              "storage_bucket": "${{ secrets.FIREBASE_PROJECT_ID }}.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "${{ secrets.FIREBASE_APP_ID }}",
                  "android_client_info": {
                    "package_name": "com.elysium.softwork"
                  }
                },
                "oauth_client": [],
                "api_key": [
                  {
                    "current_key": "${{ secrets.FIREBASE_API_KEY }}"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              }
            ],
            "configuration_version": "1"
          }
          EOF

      - name: Build Android APK
        env:
          JAVA_HOME: ${{ steps.setup-java.outputs.path }}
        run: |
          flutter build apk --debug --dart-define-from-file=lib/config.json

      - name: Upload Android to Firebase App Distribution
        uses: w9jds/firebase-action@v2.2.2
        with:
          args: >
            appdistribution:distribute
            build/app/outputs/flutter-apk/app-debug.apk
            --app ${{ secrets.FIREBASE_APP_ID }}
            --groups testers-android
        env:
          GCP_SA_KEY: ${{ secrets.FIREBASE_CREDENTIALS }}